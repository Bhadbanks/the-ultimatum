# ===========================
# WhatsApp Bot Configuration
# ===========================

# Your bot session ID (used for WhatsApp auth)
SESSION_ID=bot_session

# Optional: Enable VPS mode if your bot checks for it
VPS=true

# Optional: Bot prefix for commands (if supported)
BOT_PREFIX=!

# Optional: Logging level (info, warn, error, debug)
LOG_LEVEL=info

# Optional: External API keys (replace with your own if needed)
API_KEY=

# Optional: Any custom variables your bot uses
CUSTOM_VAR=
